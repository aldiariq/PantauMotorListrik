<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.monitoringmotorlistrik">

    <uses-permission
        android:name="android.permission.INTERNET"
        tools:remove="android:maxSdkVersion" />
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE"
        tools:remove="android:maxSdkVersion" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:remove="android:maxSdkVersion" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:remove="android:maxSdkVersion" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoutama"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_config_file"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@drawable/logoutama"
        android:supportsRtl="true"
        android:theme="@style/Theme.MonitoringMotorListrik">
        <activity
            android:name=".DashboardActivity"
            android:label="@string/title_activity_dashboard"></activity>
        <activity android:name=".LupapassActivity" android:theme="@style/Theme.MonitoringMotorListrikNoActionbar" />
        <activity android:name=".RegisterActivity" android:theme="@style/Theme.MonitoringMotorListrikNoActionbar" />
        <activity android:name=".LoginActivity" android:theme="@style/Theme.MonitoringMotorListrikNoActionbar"/>
        <activity android:name=".SplashActivity" android:theme="@style/Theme.MonitoringMotorListrikNoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>